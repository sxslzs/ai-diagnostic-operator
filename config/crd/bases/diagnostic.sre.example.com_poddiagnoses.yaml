---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: poddiagnoses.diagnostic.sre.example.com
spec:
  group: diagnostic.sre.example.com
  names:
    kind: PodDiagnosis
    listKind: PodDiagnosisList
    plural: poddiagnoses
    singular: poddiagnosis
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.podName
      name: Pod
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PodDiagnosis 是 poddiagnoses API 的 Schema
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PodDiagnosisSpec 定义了诊断任务的期望状态和目标
            properties:
              namespace:
                description: 目标 Pod 所在的命名空间
                type: string
              podName:
                description: 目标 Pod 的名称
                type: string
              tailLines:
                description: 需要向前获取的日志行数，默认可设为 100
                format: int32
                type: integer
              triggerReason:
                description: 触发诊断的原因，例如 "CrashLoopBackOff", "OOMKilled"
                type: string
            required:
            - namespace
            - podName
            type: object
          status:
            description: PodDiagnosisStatus 定义了诊断任务的实际状态和 AI 诊断结果
            properties:
              diagnosisTime:
                description: 诊断完成的时间戳
                format: date-time
                type: string
              phase:
                description: 当前诊断阶段：Pending, Diagnosing, Completed, Failed
                type: string
              rootCause:
                description: AI 总结的根本原因 (Root Cause)
                type: string
              suggestion:
                description: AI 给出的修复建议 (Suggestion)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
